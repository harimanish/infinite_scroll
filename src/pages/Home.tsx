import Layout from "../Layout";
import InfiniteScrollList from "../components/InfiniteScrollList";

const Home = () => {
    return (
        <Layout>
            <div>
                <InfiniteScrollList />
            </div>
        </Layout>
    );
};

export default Home;
